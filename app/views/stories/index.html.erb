<h1>Story一覧</h1>
<table>
  <%# <thead>
    <tr>
      <th>管理用タイトル</th>
      <th>SNS用タイトル</th>
      <th>サムネ画像</th>
      <th>タイトル</th>
      <th>画像</th>
      <th colspan="3"></th>
    </tr>
  </thead> %>

  <tbody>
    <% @story.each do |story| %>
      <tr>
        <td><%= story.admin_title %></td>
        <%# <td><%= story.title %></td> 
        <td><%= image_tag(story.thumbnail_image.url) if story.thumbnail_image && story.thumbnail_image.url %></td>
        <%# <td><% story.parts.each do |p| %> 
              <%# <%= p.text %> 
            <%# <% end %> 
        <%# </td> %>
        <%# <td><% story.parts.each do |p| %> 
              <%# <%= image_tag(p.image.url) if p.image && p.image.url %> 
            <%# <% end %> 
        <%# </td> %>
        <td><%= link_to 'Show', story %></td>
        <td><%= link_to 'Edit', edit_story_path(story) %></td>
        <td><%= link_to 'Destroy', story, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'New', new_story_path %>