<h1>Story一覧</h1>

<% @story.each do |story| %>
  <br><%= story.admin_title %>
  <br><%= image_tag(story.thumbnail_image.url) if story.thumbnail_image && story.thumbnail_image.url %></td>
  <br><td><%= link_to 'Show', story %></td>
      <td><%= link_to 'Edit', edit_story_path(story) %></td>
      <td><%= link_to 'Destroy', story, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr><br>
<% end %>

<br>
<%= link_to 'New', new_story_path %>