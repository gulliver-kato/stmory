<div class="nested-fields">
    <%= f.label :コメント %>
    <%= f.text_area :text %>
    <%= f.label :画像 %> 
    <%# <%= image_tag(@story.image.url) if @story.image && @story.image.url %> 
    <%= f.file_field :image %> 
    <%= f.hidden_field :image_cache %>
    <%= link_to_remove_association "タイトル＋画像を削除", f %>
</div>